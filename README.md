# ai-search-algorithms
Python implementations of classic AI search algorithms (BFS, DFS, and A*) applied to maze pathfinding. Demonstrates graph traversal, heuristics (Manhattan distance), and optimal path discovery in grid-based environments.
